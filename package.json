{
  "name": "webpack-analyse",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rimraf dist && webpack --mode=production --env longTermCaching=true --env googleAnalytics=true",
    "deploy": "yarn build && gh-pages -d dist -m \"Publish\" -a",
    "dev": "webpack serve --mode=development"
  },
  "dependencies": {
    "d3": "^5.15.0",
    "jquery": "^3.6.0"
  },
  "devDependencies": {
    "css-loader": "^6.7.1",
    "gh-pages": "^4.0.0",
    "imports-loader": "^4.0.0",
    "pug-plugin": "^2.8.0",
    "rimraf": "^3.0.2",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0",
    "webpack-dev-server": "^4.9.2"
  }
}
